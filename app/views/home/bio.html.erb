<div class="container">

  <div class="page-header">
    <h1>Biography for: <small> <%= params[:tweet_owner] %></small></h1>
  </div>

  <table class="table table-condensed">
    <tr>
      <th>Name</th>
      <th>Description</th>
    </tr>

    <tr>
      <td><%= @user.name.presence || 'No name.' %></td>
      <td><%= @user.description.presence || 'No Description.' %></td>
    <tr>
  </table>

  <div>
    <%= link_to 'back', tweets_path(trend_name: params[:trend_name]) %> |
    <%= link_to 'home', home_path %></td>
  </div>

</div>